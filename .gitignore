node_modules/
.env
.DS_Store
.idea/
*.log

# Solana keypairs — never commit
target/deploy/*-keypair.json
id.json

# Face data (stored on Render persistent disk)
face_service/face_data/

# Admin key persistence (server-side, not for version control)
data/

# Anchor build artifacts
target/
